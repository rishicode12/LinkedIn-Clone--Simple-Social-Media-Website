{
  "name": "linkedin-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix linkedin-frontend",
    "build": "npm run build --prefix linkedin-frontend",
    "start": "npm run start --prefix server",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix linkedin-frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}