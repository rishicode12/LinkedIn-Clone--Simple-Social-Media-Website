{
  "name": "linkedin-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix linkedin-frontend\"",
    "start": "concurrently \"npm start --prefix server\" \"npm run preview --prefix linkedin-frontend\"",
    "build": "npm run build --prefix linkedin-frontend",
    "install-all": "npm install && npm install --prefix server && npm install --prefix linkedin-frontend",
    "postinstall": "npm install --prefix server && npm install --prefix linkedin-frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "react-router-dom": "^7.9.5"
  }
}